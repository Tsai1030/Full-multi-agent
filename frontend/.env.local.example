# 複製為 .env.local（已被 git 忽略）
# 後端 API 位址（預設即可）
NEXT_PUBLIC_API_URL=http://localhost:8000

# Google 登入用的 Client ID（與後端 GOOGLE_OAUTH_CLIENT_ID 同一個值）
# 沒設定時，登入頁的 Google 按鈕會自動隱藏，其餘功能不受影響
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
